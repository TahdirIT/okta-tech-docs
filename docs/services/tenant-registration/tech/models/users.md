# `users` (Owner account) — تم نقلها إلى User Management

تم فصل توثيق نموذج المستخدم إلى خدمة مستقلة:

- `docs/services/user-management/tech/models/users.md`

ضمن `tenant-registration` نستخدم نفس نموذج المستخدم لإنشاء/ربط **المستخدم المالك (Owner/Admin)** أثناء تدفق التسجيل.

