from: auth-context
flows:
  - to: login
    type: auth-redirect
    method: GET
    middleware: [auth]
    source: livewire

  - to: dashboard
    type: form
    method: POST
    middleware: [auth]
    source: livewire
